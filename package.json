{
  "name": "govuk-template-sass",
  "version": "1.0.0",
  "description": "Bundles up the sass and assets for the Gov UK tempalte https://github.com/alphagov/govuk_template",
  "main": "index.js",
  "scripts": {
    "start": "npm run init:submodule && npm run update:submodule && npm run bundle",
    "bundle": "npx gulp",
    "init:submodule": "git submodule init",
    "update:submodule": "git submodule update",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "meyric@dxw.com",
  "license": "MIT",
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-cli": "^2.0.1",
    "gulp-rename": "^1.2.2"
  }
}
